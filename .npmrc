# Refuse to install under a Node version that violates `engines` in package.json.
# The local default was Node 16 (EOL) at project start; this turns that class of
# mistake into an immediate, obvious failure rather than a confusing one later.
engine-strict=true
